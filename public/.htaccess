RewriteEngine On
RewriteBase /

# Never rewrite PHP scripts or API endpoints
RewriteRule ^.*\.php$ - [L]
RewriteRule ^api/ - [L]

# Serve existing files and directories directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
